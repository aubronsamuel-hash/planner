# Infrastructure Scaffolding

This folder mirrors the layout documented in the Planner blueprint. Use the
Compose files for local development (`docker-compose.dev.yml`) and deployment
design discussions (`docker-compose.prod.yml`). Dockerfiles provide minimal
runtime images; replace placeholder commands as services mature. Helm and
Terraform directories are intentionally empty pending Phase 2 provisioning.
