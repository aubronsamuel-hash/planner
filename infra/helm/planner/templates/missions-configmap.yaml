apiVersion: v1
kind: ConfigMap
metadata:
  name: planner-missions-config
  labels:
    app.kubernetes.io/name: planner
    app.kubernetes.io/component: missions
data:
  FEATURE_MISSIONS_ENABLED: "true"
